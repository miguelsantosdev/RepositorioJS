// função de multiplacação

let v = function(x){ // v recebe parametro x
    return x*2 // Ação: O numero que está em X será multiplicado por 2
}
console.log(v(15)) //Chamada: Acrescente o numero dentro de v() para o parametro e escreva na tela.